# 2.4 unread badges — CLOSING DESK repo (closing-desk)
Drop into src/ (REPLACE both):
  - PartnerWorkspace.jsx  (red count badge on matter rows w/ new Lakeland messages)
  - partnerDb.js          (adds markRead + listUnreads)
Opening a matter marks its thread read and clears the badge.
