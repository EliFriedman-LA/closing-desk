# 3.1a Deadlines — CLOSING DESK repo (closing-desk)
SQL already run. Drop into src/ (REPLACE both):
  - PartnerWorkspace.jsx  (new "Deadline setup" nav page + Deadlines panel on each matter)
  - partnerDb.js          (deadline template + matter-deadline helpers)

Try it:
  1. Sidebar -> "Deadline setup" -> "Load NJ starter set" (or build your own rows).
  2. Open a matter -> Deadlines panel -> set Contract/Closing dates -> "Generate from template".
  3. Check off / add / edit / delete deadlines per matter. Overdue dates show in red.
